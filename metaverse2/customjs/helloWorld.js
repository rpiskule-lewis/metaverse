function update (scene) {
    console.log("hi")
/*
    if (this.position.x - camera.position.x > 1) {
        myPosition = -1
    }
    if (this.position.x - camera.position.x < -1) {
        myPosition = 1
    }
    if (this.position.z - camera.position.z > 1) {
        myPosition = -1
    }
    if (this.position.z - camera.position.z < -1) {
        myPosition = 1
    }
    */
}

export {update}